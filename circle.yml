machine:
  environment:
    TRAVIS: "true"

checkout:
  post:
    - git submodule sync
    - git submodule update --init

test:
  override:
    - cd nix; autoreconf -fi
    - cd nix; ./configure --enable-generic --without-gtk2
    - cd nix; make anew
