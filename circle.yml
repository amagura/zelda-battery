machine:
  timezone:
    America/Denver

checkout:
  post:
    - git submodule sync
    - git submodule update --init

test:
  environment:
    TRAVIS: "true"
  override:
    timeout: 300
    - cd nix; autoreconf -fi
    - cd nix; ./configure --enable-generic --without-gtk2
    - cd nix; make anew
