machine:
  environment:
    TRAVIS: "true"

checkout:
  post:
    - git submodule sync
    - git submodule update

test:
  override:
    - autoreconf -fi
    - ./configure --enable-generic --without-gtk2
    - make
