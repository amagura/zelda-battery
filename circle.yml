machine:
  timezone:
    America/Denver

checkout:
  post:
    - git submodule sync
    - git submodule update --init

test:
  timeout: 300
  environment:
    TRAVIS: "true"
  override:
    - cd nix; autoreconf -fi
    - cd nix; ./configure --enable-generic --without-gtk2
    - cd nix; make anew
